<%= form_for(social_share) do |f| %>
  <% if social_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(social_share.errors.count, "error") %> prohibited this social_share from being saved:</h2>

      <ul>
      <% social_share.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :solicitation_id %>
    <%= f.text_field :solicitation_id %>
  </div>

  <div class="field">
    <%= f.label :type %>
    <%= f.text_field :type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
